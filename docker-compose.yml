version: "3.8"
services:
  php:
    build:
      context: .
      dockerfile: php/Dockerfile
    container_name: user-lorem-ipsum-php
    volumes:
      - ./src:/var/www
    networks:
      - web
  
  nginx:
    image: nginx:alpine
    container_name: user-lorem-ipsum-nginx
    ports:
      - 8000:80
    volumes:
      - ./src:/var/www
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php
    networks:
      - web
networks:
  web:
    driver: bridge